services:
  echoer:
    cmd: sh
    args:
      - -c
      - echo hello && sleep 2
    on_exit:
      type: Shutdown
  echoer2:
    cmd: sleep
    args: ["10"]
    on_exit:
      type: Restart
      restart_delay: 1
